import { Injector, Type } from '@angular/core';
import { NgElementStrategy, NgElementStrategyFactory } from '@angular/elements';
export declare class ElementZoneStrategyFactory implements NgElementStrategyFactory {
    private component;
    private injector;
    static counter: number;
    private ngElement;
    protected string4(): string;
    protected generateName(): string;
    constructor(component: Type<any>, injector: Injector);
    create(injector: Injector): NgElementStrategy;
}
