import { Injector } from '@angular/core';
import { Observable } from 'rxjs';
import { NgElementStrategyEvent, NgElementStrategy } from '@angular/elements';
export declare class ElementZoneStrategy {
    protected strategy: NgElementStrategy;
    protected injector: Injector;
    private ngZone;
    events: Observable<NgElementStrategyEvent>;
    constructor(strategy: NgElementStrategy, injector: Injector);
    connect(element: HTMLElement): void;
    disconnect(): void;
    getInputValue(propName: string): any;
    setInputValue(propName: string, value: string): void;
    private runInZone(fn);
}
